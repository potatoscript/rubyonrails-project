<%= form_with(model: [job, job.comments.build]) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :body %>
        <%= f.submit "Reply", class:"btn btn-primary mt-1" %>
    </div>
<% end %>